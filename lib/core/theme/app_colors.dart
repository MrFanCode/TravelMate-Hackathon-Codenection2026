import 'package:flutter/material.dart';

/// TravelMate design tokens — "passport & airmail" palette.
/// Keep these as the single source of truth; don't hardcode hex values
/// elsewhere in the app.
class AppColors {
  AppColors._();

  static const paper = Color(0xFFF3E9D2);
  static const paper2 = Color(0xFFEDE0BF);
  static const ink = Color(0xFF22314A);
  static const inkSoft = Color(0xFF5B5142);
  static const rust = Color(0xFFB84A2E);
  static const rustDark = Color(0xFF8E3821);
  static const mustard = Color(0xFFD89B34);
  static const pine = Color(0xFF3B5C4C);
  static const line = Color(0xFFCBB98A);
  static const cream = Color(0xFFFBF6E9);
}
